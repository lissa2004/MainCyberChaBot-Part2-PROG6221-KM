<Window x:Class="MainCyberSecurityChatBot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CyberSecurity ChatBot"
        Height="600"
        Width="900">

    <Grid Background="#101820">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="799*"/>
            <ColumnDefinition Width="101*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="90"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Background="#89056c"
                Margin="10,10,10,10"
                CornerRadius="15" Grid.ColumnSpan="2">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="CYBER SECURITY BOT"
                           FontSize="30"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Foreground="White"
                />

                <TextBlock Text="Stay Safe In The Digital World"
                           FontSize="16"
                           HorizontalAlignment="Center"
                           Foreground="White"
                />
            </StackPanel>
        </Border>

        <!-- CHAT AREA -->
        <ScrollViewer Grid.Row="1"
                      Margin="10,10,10,10" Grid.ColumnSpan="2">
            <StackPanel x:Name="ChatPanel"/>
        </ScrollViewer>

        <!-- INPUT AREA -->
        <Grid Grid.Row="2"
              Margin="10,10,10,10" Grid.ColumnSpan="2">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                 CornerRadius="15"
                 Background="#EEEEEE"
                 Margin="0,0,10,0"
                 Padding="5">

                <TextBox x:Name="UserInput"
                    FontSize="16"
                    BorderThickness="0"
                    Background="Transparent"
                    Foreground="Black"
                />
            </Border>
            <Button Grid.Column="1"
                 Content="SEND"
                 FontSize="16"
                 FontWeight="Bold"
                 Background="#89056c"
                 Foreground="White"
                 BorderThickness="0"
                 Click="SendButton_Click"
                 Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border"
                                         Background="{TemplateBinding Background}"
                                         CornerRadius="15">
                                        <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                        />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver"
                                             Value="True">
                                            <Setter TargetName="border"
                                                 Property="Background"
                                                 Value="#00CFC8"
                                        />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Grid>
</Window>